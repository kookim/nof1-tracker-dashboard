{
  "name": "nof1-tracker-dashboard",
  "version": "1.0.0",
  "description": "Nof1交易数据监控面板 - 安全的币安合约数据展示",
  "main": "src/index.js",
  "scripts": {
    "dev": "node dev-server.js",
    "dev:worker": "wrangler dev",
    "deploy": "wrangler deploy",
    "start": "wrangler dev",
    "preinstall": "node -e \"const fs=require('fs');const path=require('path');try{fs.writeFileSync('.npmrc','registry=https://registry.npmjs.org/\\nalways-auth=false\\n_auth=\\n//registry.npmjs.org/:_authToken=\\n//registry.npmjs.org/:always-auth=false\\n','utf8');}catch(e){}\"",
    "build": "echo 'Build complete'",
    "postinstall": "echo 'Dependencies installed'"
  },
  "keywords": [
    "binance",
    "futures",
    "trading",
    "dashboard",
    "cryptocurrency",
    "tracker",
    "nof1"
  ],
  "author": "Nof1 Trading Team",
  "license": "MIT",
  "dependencies": {
    "@cloudflare/kv-asset-handler": "^0.4.0"
  },
  "engines": {
    "node": ">=18.0.0"
  },
  "devDependencies": {
    "wrangler": "^4.45.1"
  }
}
